# ============================================================
#  URAx-LACE — ALQAC 2026 pipeline dependencies
#  Tested target: Google Colab (L4 / A100), Python 3.10/3.11, CUDA 12.x
# ------------------------------------------------------------
#  Install order matters a little on Colab; run_colab.sh handles it.
#  Versions are pinned loosely (>=) so Colab's pre-installed torch is reused.
# ============================================================

# --- Inference engine (fast, batched) ---
vllm>=0.6.3               # serves Qwen3-8B with guided/JSON decoding & prefix caching
transformers>=4.51.0      # fallback backend + tokenizers (Qwen3 needs >=4.51)
accelerate>=0.34.0

# --- Retrieval / embeddings ---
FlagEmbedding>=1.3.0      # BAAI/bge-m3 dense+sparse multilingual embedder
sentence-transformers>=3.0.0
faiss-cpu>=1.8.0          # ANN index for precedent & law retrieval (cpu is plenty)
rank-bm25>=0.2.2          # lexical retrieval over law corpus

# --- Utils ---
requests>=2.31.0
numpy>=1.26.0
tqdm>=4.66.0
pyyaml>=6.0
natsort>=8.4.0
regex>=2024.5.15
orjson>=3.10.0            # fast json for large corpus files
